services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/users.db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  db-data:
